# go-scaler Agent 工作流规范

本文件面向 Claude / Cursor 等 Agent：在 `go-scaler/` 目录内工作时请遵守以下规则。

---

## 规则 1 — 编码前先建 `<任务名>_TODO.md`

- 收到任务后，在 `docs/` 目录下以 `<任务名>_TODO.md` 命名创建任务文件，列出任务分解。
- **命名**：用能概括本次任务的英文或拼音短语，例如 `add_rate_limit_TODO.md`。
- **清单格式**：`- [ ] 未完成` / `- [x] 已完成`。
- **同一任务**：若 `docs/` 下已有文件名与本次需求语义高度一致（同一功能/同一需求），则向该文件**追加**条目，不要新建重复文件；否则新建独立文件。
- 不得整文件覆盖已有内容；不同任务不要混进同一个 `_TODO.md`。

### `<任务名>_TODO.md` 格式约定

```markdown
# <任务名称>

> 创建时间：YYYY-MM-DD

## 任务列表

- [ ] 子任务 A
- [ ] 子任务 B
- [x] 已完成的子任务
```

### 同一任务判断标准

| 情况 | 处理 |
|------|------|
| 用户明确说「继续上次的 xxx 任务」 | 追加到对应文件 |
| 任务描述与已有 `_TODO.md` 文件名语义高度重叠（同功能模块、同需求） | 追加到已有文件 |
| 全新功能、不同模块、明显不同需求 | 新建独立文件 |
| 不确定是否同一任务 | 主动询问用户 |

### 及时更新 `_TODO.md`

- 每完成一个可独立追踪的小 TODO（如一个子任务、一次验证、一次文档同步），应立即将对应条目标为 `[x]`，并视需要补充简短备注（如关键文件路径、验证命令、PR 链接）。
- 不要等到整个任务全部完成后再统一更新 `_TODO.md`；这样便于用户实时查看进度，也便于 Agent 在中断或恢复上下文后快速接续工作。

---

## 规则 2 — 方案确认前不写代码

在以下任一情况成立时，必须先与用户沟通并确认方案，**在得到明确确认前不要编写或修改业务代码**（只读探索、查阅文档除外）：

- 需求存在多种合理实现路径；
- 涉及对外 API、配置结构、数据契约的变更；
- 影响范围明显超出单个函数或小范围局部修改。

用户明确确认（例如「确认」「可以」「go」等）后，再开始编码。

---

## 规则 3 — 同步更新 README.md

当本次变更对使用者**可见**时，须在交付前更新本目录下的 [README.md](README.md)：

- **需要更新**：API 接口、配置项、环境变量、CLI 参数、部署/运行步骤等发生可见变更。
- **可不更新**：纯内部重构、仅 bug 修复且对外行为不变、仅注释或内部文档微调。

更新应在相关代码变更完成后一并完成，避免文档滞后。
